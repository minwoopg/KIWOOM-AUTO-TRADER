# 주식 자동매매 프로그램 ver1.3 패치노트

> 작성일: 2026-05-21

---

## 작업 개요

이번 ver1.3에서는 ver1.2의 장세 분류 기반 전략 선택 구조 위에 다음 개선 사항을 추가하였습니다.

- 트레일링 스탑 + 추세 꺾임 기반 매도 전략 도입 (고정 익절 제거)
- NEUTRAL 장세 추가 — 기존 SIDEWAYS를 세분화하여 매수 기회 확대
- REBOUND 장세 + 바닥권 매수 전략(BottomStrategy) 신규 추가
- 매수 모드 3가지로 분리 — 돌파 / 눌림목 / 반등별 조건 독립 적용
- 단일 전략 통합 — 단타/스윙 이중 구조 제거
- 운영 안정성 개선 — 트레일링 최고가 저장, 장 마감 처리, 일일 리포트 개선

---

## 1. 매도 전략 전면 개편 — 트레일링 스탑 + 추세 꺾임

### 1.1 배경

기존 전략은 고정 익절 +2%를 사용하였습니다. 이 방식은 단순하고 예측 가능하지만, 추세가 강한 종목을 너무 일찍 팔아버리는 문제가 있었습니다.

예시: 010170(LS)이 +7%까지 상승 가능한 날, +2%에서 익절하여 수익 기회를 대부분 놓침

### 1.2 변경 내용

고정 익절을 완전히 제거하고, 아래 4단계 매도 로직으로 전환하였습니다.

| 순위 | 조건 | 설명 |
|---|---|---|
| ① | 손절 -1.5% | 평균단가 대비 즉시 손절 (최우선) |
| ② | 트레일링 스탑 | 최고가 대비 -2% 하락 시 매도 (최소 +1% 이후 작동) |
| ③ | 추세 꺾임 | RSI ≥ 70 + RSI 하락전환 + MACD 히스토그램 축소 |
| ④ | 안전망 익절 | 평균단가 대비 +15% (급등 시 확보) |

### 1.3 실제 동작 예시

```
매수  21,500원
최고가 23,000원 (+7.0%) → 트레일링 추적 중
하락  22,540원 (최고가 대비 -2.0%) → 트레일링 스탑 발동
실현  +4.8%  ← 고정 +2%보다 2.4배 더 수익
```

### 1.4 관련 설정 추가

```yaml
trailing_stop_pct: 2.0      # 최고가 대비 -2% 하락 시 매도
trailing_start_pct: 1.0     # 최소 +1% 이상 올랐을 때부터 작동
trend_reversal_rsi: 70.0    # 추세 꺾임 RSI 기준
take_profit_pct: 15.0       # 안전망 익절 기준 (급등 시)
```

### 1.5 트레일링 최고가 state.json 저장

프로그램 재시작 시 최고가 정보가 사라지는 문제를 해결하였습니다.

- `state_store.py` — `load()` 시 `highest_price` 함께 복원
- `state_store.py` — `save()` 시 `highest_price` 함께 저장
- `trading_service.py` — 시작 시 state.json에서 최고가 복원

---

## 2. NEUTRAL 장세 추가 — 기존 SIDEWAYS 세분화

### 2.1 배경

기존에는 BULLISH가 아니면 모두 SIDEWAYS(완전 관망)로 처리하였습니다. RSI 40~60 구간처럼 추세가 애매한 날에도 매수 기회를 전혀 잡지 못하는 문제가 있었습니다.

실제 로그에서 101680 종목이 하루 종일 SIDEWAYS로 분류되어 B/C 조건을 충족하는 구간에서도 매수가 차단되는 현상이 반복되었습니다.

### 2.2 장세 구조 변경

| 장세 | 조건 | 전략 | 허용 매수 |
|---|---|---|---|
| BULLISH | MA↑ + RSI 정상 + MACD 골든 | BreakoutStrategy | A돌파 + B반등 + C눌림목 |
| NEUTRAL | RSI 35~65 + 추세 불명확 | NeutralStrategy | B반등 + C눌림목만 (3점↑) |
| REBOUND | RSI≤35 + RSI Signal 골든 + MACD반전 | BottomStrategy | 바닥권 안전 매수 |
| SIDEWAYS | 극단값 또는 과매수 | HoldStrategy | 완전 관망 |
| BEARISH | MA↓ + RSI↓ + MACD 데드 | HoldStrategy | 완전 관망 |
| UNKNOWN | 데이터 부족 | HoldStrategy | 보수적 관망 |

### 2.3 NeutralStrategy 매수 조건

- B조건 (저점 반등): 저점 대비 +2% 이상 + VWAP 위
- C조건 (눌림목): 등락률 -1%~-8% + MA5 > MA20 + VWAP 위
- 점수제: 6점 중 3점 이상 (BULLISH의 2점보다 엄격)
- A조건(상승 돌파) 불허 — 추세 불명확 구간에서 고가 추격은 위험

---

## 3. REBOUND 장세 + 바닥권 매수 전략 추가

### 3.1 배경

기존 전략은 이미 오르고 있는 종목을 추격하는 구조였습니다. 충분히 하락 후 반등 초입 종목을 잡는 전략이 없어 바닥권 매수 기회를 전혀 활용하지 못하였습니다.

### 3.2 REBOUND 장세 판정 조건

- RSI(14) ≤ 35 — 과매도 구간
- RSI Signal(9) 골든크로스 — RSI가 9일 EMA를 상향 돌파
- MACD 히스토그램 반전 — 음수 구간에서 증가 시작
- MA5 < MA20 (아직 상승 추세 아님) — 바닥권 조건

### 3.3 BottomStrategy 매수 조건

필수 3가지를 모두 충족해야 매수합니다.

| 구분 | 조건 | 설명 |
|---|---|---|
| 필수① | RSI < 35 + Signal 골든크로스 | 과매도 + RSI 방향 전환 |
| 필수② | MACD 히스토그램 반전 | 모멘텀 바닥 확인 |
| 필수③ | 거래량 시나리오 A 또는 B | A: 매물고갈(거래량 < 평균 70%) / B: 세력유입(거래량 > 평균 130%) |
| 선택④ | 분봉 저점 상승 또는 VWAP 근접 | 반등 초입 확인 |

### 3.4 BottomStrategy 매도 조건

- 손절: 평균단가 -1.5%
- 트레일링: +3% 이상부터 작동, 최고가 대비 -2%
- 안전망: 평균단가 +15%

### 3.5 새로 추가된 지표

| 지표 | 설명 | 용도 |
|---|---|---|
| `indicator_rsi_signal` | RSI의 9일 EMA | REBOUND 판정 |
| `indicator_rsi_signal_cross` | +1 골든크로스 / -1 데드크로스 | REBOUND 판정 |
| `indicator_volume_exhaustion` | 거래량 < 20일 평균 70% | 매물 고갈 시나리오 |
| `indicator_volume_buying` | 거래량 > 20일 평균 130% | 세력 유입 시나리오 |
| `is_valid_pulldown` | 조건 C 눌림목 유효 여부 | NEUTRAL/BULLISH 매수 |
| `ma5_above_ma20` | MA5 > MA20 여부 | 상승 추세 확인 |

---

## 4. 매수 모드 3가지 분리

### 4.1 배경 — 핵심 문제

기존 구조의 가장 큰 문제는 다음과 같았습니다.

> A조건(상승 추격)을 통과해도, 고가 대비 -1%~-7% 눌림목을 다시 요구함

삼성전자(005930)가 +8.3% 급등한 날, 고가 대비 -0.2%라는 이유로 하루 종일 매수 차단되는 문제가 발생하였습니다.

### 4.2 변경 전후 비교

| 모드 | 등락 조건 | 눌림목 조건 | 점수 기준 |
|---|---|---|---|
| A 상승 돌파 | +2%~+18% | 고가 대비 -2% 이내 허용 | 2점↑ |
| B 저점 반등 | 제한 없음 | 없음 (저점 확인으로 대체) | 2점↑ |
| C 눌림목 | -1%~-8% | 고가 대비 -1%~-7% 유지 | 2점↑ |

### 4.3 실제 효과

```
변경 전: 005930 등락 +8.3%, 고가 대비 -0.2% → 하루 종일 BLOCK
변경 후: 005930 등락 +8.3%, 고가 대비 -0.2% → A조건 통과 → 매수 가능
```

---

## 5. 단일 전략 통합 — 단타/스윙 이중 구조 제거

### 5.1 배경

기존에는 `day_symbols`(단타)와 `swing_symbols`(스윙)를 분리하여 관리하는 구조였습니다. 그러나 스윙 종목 목록이 항상 비어있었고, 트레일링 스탑 도입 이후 두 전략의 차이가 없어졌습니다.

### 5.2 제거된 것

- `SwingConfig`, `swing_symbols`, `day_symbols` 분리 제거
- `StrategyRouter`의 `is_swing` 분기 제거
- `_try_buy`의 단타/스윙 포지션 분리 체크 제거
- 장 마감 강제청산의 스윙 예외 처리 제거
- `settings.yaml`의 `swing` 섹션 제거

### 5.3 단순해진 구조

```yaml
targets:
  symbols:        # 종목 하나의 리스트로 통일
    - "001510"
    - "047040"
```

---

## 6. 운영 안정성 개선

### 6.1 장 마감 처리 개선

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 강제청산 시작 | 15:20 (10분 전) | 15:18 (12분 전, 단일가 진입 전) |
| 429 백오프 (장 마감 20분 내) | 180초 | 10초 (타이밍 놓침 방지) |
| 일일 리포트 생성 | 강제청산 성공 시만 | 강제청산 실패해도 반드시 생성 |

### 6.2 일일 리포트 개선

- `trades.csv`에 `price`(체결가) 필드 추가
- 실현 손익 계산 (매수가 × 수량 vs 매도가 × 수량)
- 승률 표시 (N승 N패 XX%)
- 종목별 상세 내역 (매수가 → 매도가, 손익률, 홀딩 중 표시)
- 평균 보유 시간 계산

### 6.3 가격 캐시 및 주문 금액 조정

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 가격 캐시 | 300초 (5분) | 60초 (1분) |
| 주문 금액 | 20만원 | 50만원 |

### 6.4 조건 C (눌림목 매수) 추가

하락장에서 눌림목 종목을 잘 못 잡는 문제를 해결하기 위해 조건 C를 신규 추가하였습니다.

- 등락률 -1% ~ -8% (오늘 하루 조정)
- MA5 > MA20 필수 (상승 추세 중인 종목만)
- VWAP 위에 있어야 함

이를 통해 분봉 필터가 A(상승 추격) / B(저점 반등) / C(눌림목) 3가지로 확장되었습니다.

---

## 7. 현재 버전 상태

### 7.1 전략 구조 전체 흐름

| 단계 | 내용 |
|---|---|
| 1. 장세 판단 | BULLISH / NEUTRAL / REBOUND / SIDEWAYS / BEARISH / UNKNOWN |
| 2. 전략 선택 | 장세별 자동 선택 (StrategyRouter) |
| 3. 분봉 필터 | A/B/C 조건 + 거래대금 ≥ 10억 |
| 4. 점수제 | 6점 중 N점 이상 (장세별 기준 다름) |
| 5. 매도 | 트레일링 스탑 + 추세 꺾임 + 안전망 |

### 7.2 현재 설정값

| 설정 | 값 | 설명 |
|---|---|---|
| `order_cash_per_trade` | 500,000원 | 1회 주문 금액 |
| `max_positions` | 5종목 | 최대 동시 보유 |
| `stop_loss_pct` | 1.5% | 손절 기준 |
| `trailing_stop_pct` | 2.0% | 트레일링 스탑 |
| `trailing_start_pct` | 1.0% | 트레일링 시작 기준 |
| `take_profit_pct` | 15.0% | 안전망 익절 |
| `trend_reversal_rsi` | 70.0 | 추세 꺾임 RSI 기준 |
| `force_exit_minutes` | 12분 | 강제청산 시작 (15:18) |
| `price_refresh_seconds` | 60초 | 가격 캐시 주기 |

### 7.3 로그 태그 체계

| 태그 | 의미 |
|---|---|
| `[BUY]` | 매수 신호 발생 |
| `[SELL]` | 매도 신호 발생 |
| `[HOLD_POS]` | 보유 중 (수익률 + 트레일링 진행상황) |
| `[NEAR_TP]` | 익절가 2% 이내 근접 경고 |
| `[NEAR_SL]` | 손절가 1% 이내 근접 경고 |
| `[BLOCK]` | 분봉 필터 차단 (사유 명시) |
| `[HOLD]` | 일반 홀딩 (장세 불일치) |
| `[MIN]` | 분봉 분석 결과 |
| `[ORDER]` | 주문 접수 완료 |
| `[FAIL]` | 주문 실패 |
| `[COOL]` | 재진입 쿨다운 중 |
| `[EXCL]` | UNKNOWN 3회 → 자동 제외 |
| `[REGIME]` | 장세 판단 결과 |

---

## 8. 다음 버전 예정 방향

| 우선순위 | 항목 | 내용 |
|---|---|---|
| 🔴 높음 | 텔레그램 알림 | 매수/매도 발생 시 폰 알림 + 일일 리포트 자동 발송 |
| 🔴 높음 | max_order_amount 수정 | 30만 → 60만원 (50만 주문 차단 문제 해결) |
| 🟡 중간 | 모드별 매수 금액 차등 | BULLISH 50만 / NEUTRAL 30만 / REBOUND 20만 |
| 🟡 중간 | HTS 조건검색식 개선 | REBOUND용 조건식 별도 추가 |
| 🟢 낮음 | 분할 익절 추가 | +7% 시 50% 매도 후 나머지 트레일링 |
| 🟢 낮음 | RSI Wilder 방식 개선 | 현재 단순 평균 → Wilder 방식으로 정확도 향상 |

---

## 요약

이번 ver1.3 작업을 통해 다음이 완료되었습니다.

- 트레일링 스탑 + 추세 꺾임 매도로 수익 극대화 구조 완성
- NEUTRAL / REBOUND 장세 추가로 매수 기회 대폭 확대
- 바닥권 매수 전략(BottomStrategy) 신규 추가
- 매수 모드 A/B/C 분리로 강한 종목 차단 문제 해결
- 단타/스윙 이중 구조 제거로 코드 단순화
- 트레일링 최고가 state.json 저장으로 재시작 안전성 확보
- 장 마감 처리 및 일일 리포트 개선

ver1.3은 단순 매수/매도 조건 개선을 넘어, 장세별로 최적화된 전략을 자동 선택하고 수익을 극대화하는 구조가 완성된 버전입니다.
