# 주식 자동매매 프로그램 ver1.4 패치노트

> 작성일: 2026-05-29

---

## 작업 개요

이번 ver1.4에서는 ver1.3의 트레일링 스탑 + 추세 꺾임 매도 구조 위에 다음 개선 사항을 추가하였습니다.

- 검증 인프라 구축 — 1분봉 저장, 리플레이, 분석 스크립트 자동 실행
- 조건검색 3개 동시 구독 — 돌파형A / 눌림목PR / V자BV
- 매도 로직 개선 — 점수제 매도 + 구간형 트레일링
- 동일 종목 재진입 제한 — 손절/트레일링 손실 반복 방어
- 분석 세분화 — SKIP_NO_PATTERN 세분화, V자 실패 사유 로그
- 손익 계산 개선 — 잔고API 평균매입단가 기반, 전일 이월 분리

---

## 1. 검증 인프라 구축

### 1.1 1분봉 저장 (MinuteBarSaver)

장중 모니터링 종목의 1분봉 데이터를 자동 저장합니다.

```
data/minute_bars/YYYYMMDD/{symbol}.csv
```

- `cntr_tm, open, high, low, close, volume` 형식으로 저장
- 품질 검증: timestamp 중복/정렬, OHLC 값/논리 자동 확인

### 1.2 리플레이 엔진 (replay_runner.py)

저장된 1분봉으로 BUY 신호 발생 후 실제 수익률을 소급 분석합니다.

주요 기능:
- 비용 반영 (왕복 수수료 0.25% + 슬리피지 0.10% = 총 0.35%)
- 5분/10분/20분 후 순수익 계산
- 패턴 조합별 성과 (A/B/C/V/PR 단독 및 조합)
- 시간대별 성과 (09:00~10:00 / 10:00~13:30 / 13:30~14:50 / 14:50~)
- 고위험 종목 자동 경고 (`WATCHLIST_EXCLUDE_CANDIDATE`)
- 전 종목 요약 섹션

### 1.3 15:25 자동 분석 실행

장 마감 후 아래 스크립트가 순서대로 자동 실행됩니다.

```
일일 리포트 생성 (15:25)
→ validate_logs (로그 품질 검증)
→ analyze_signal_log (시그널 분석)
→ analyze_trades (거래 분석)
→ replay_runner (리플레이)
```

결과 파일:
```
reports/signal_analysis_YYYYMMDD.txt
reports/trade_analysis_YYYYMMDD.txt
reports/replay_YYYYMMDD.txt
reports/minute_bar_quality_YYYYMMDD.txt
```

---

## 2. 조건검색 3개 동시 구독

### 2.1 배경

기존에는 조건검색식 1개만 구독하는 구조였습니다. 패턴별로 특화된 조건식이 필요하여 3개로 분리하였습니다.

### 2.2 조건검색식 구성

| 번호 | 이름 | 목적 |
|---|---|---|
| 1 | 자동매매_눌림목_PR | PR/C 패턴 후보 종목 |
| 2 | 자동매매_VX_BV | V자 반등 후보 종목 |
| 3 | 자동매매_돌파형A | A/B 돌파 패턴 후보 종목 |

### 2.3 동작 방식

- 3개 조건식 합집합으로 targets 관리
- 한 조건식에서 편출돼도 다른 조건식 편입 상태 유지
- 로그에 어느 조건식에서 편입/편출됐는지 표시

```
[COND] [자동매매_돌파형A] 편입: 009150
[COND] [자동매매_눌림목_PR] 편출: 018880 (다른 조건식 유지)
```

### 2.4 설정 변경

```yaml
# 변경 전
websocket:
  condition_seq: 0

# 변경 후
websocket:
  condition_seqs: [1, 2, 3]
```

---

## 3. 매도 로직 개선

### 3.1 추세꺾임 매도 — 점수제 도입

기존 즉시 반응형 매도를 점수제로 전환하였습니다.

**변경 전:**
```
RSI ≥ 70 + RSI 하락전환 + MACD 축소 → 즉시 매도
```

**변경 후:**
```
보유 수익 +0.5% 미만 → 추세꺾임 비활성 (손절만 작동)
보유 수익 +0.5% 이상 → 5개 조건 점수 계산

  +1점  RSI ≥ 70
  +1점  RSI 하락 전환
  +1점  MACD 히스토그램 축소
  +1점  VWAP 아래 이탈
  +1점  MA5 아래 이탈

3점 이상 AND (VWAP 이탈 OR MA5 이탈) → 매도
```

효과:
- 진입 직후 노이즈 매도 방지
- 가격이 실제로 무너졌을 때만 매도
- 0분 청산 케이스 감소

### 3.2 구간형 트레일링 스탑

기존 단일 -2% 트레일링을 수익 구간별 차등 적용으로 변경하였습니다.

| 고점 대비 수익 | 트레일링 폭 |
|---|---|
| +0.5 ~ +1.0% | -0.8% |
| +1.0 ~ +2.0% | -1.2% |
| +2.0 ~ +3.0% | -1.5% |
| +3.0% 이상 | -2.0% |

효과:
- 약수익 후 큰 손실 전환 방지
- 수익이 클수록 더 넓은 흔들림 허용
- 트레일링 시작 기준: +0.5% (기존 +1.0%에서 완화)

### 3.3 관련 설정 변경

```yaml
# trailing_start_pct는 코드에서 0.5%로 고정
# trailing_stop_pct는 구간별 동적 계산으로 대체
```

---

## 4. 동일 종목 재진입 제한

### 4.1 배경

같은 종목에서 반복 손실이 발생하는 패턴을 방지합니다.

실제 케이스: 018880 당일 3회 진입, 전부 손실 (-83,880원)

### 4.2 제한 규칙

| 조건 | 제한 |
|---|---|
| 손절 발생 | 해당 종목 30분 매수 금지 |
| 트레일링 손실 60분 내 2회 | 해당 종목 60분 매수 금지 |
| 당일 손실 2회 | 당일 해당 종목 매수 금지 |

### 4.3 로그 태그

| 태그 | 의미 |
|---|---|
| `[LOSS_CNT]` | 당일 손실 횟수 기록 |
| `[SYMBOL_COOLDOWN]` | 쿨다운 발동 (이유/만료시각/손실횟수) |
| `[SYMBOL_BLOCK_TODAY]` | 당일 매수 금지 발동 |

```
[SYMBOL_COOLDOWN] 018880 reason=STOPLOSS cooldown_until=10:35 loss_count=1
[SYMBOL_BLOCK_TODAY] 018880 reason=DAILY_LOSS_2 loss_count_today=2 block_buy=true
```

### 4.4 일일 초기화

장 시작 시 당일 손실 카운트를 자동 초기화합니다.

---

## 5. 분석 세분화

### 5.1 SKIP_NO_PATTERN 세분화

기존 `SKIP_NO_PATTERN` 하나로 묶이던 패턴 미충족 사유를 세분화하였습니다.

| 코드 | 의미 |
|---|---|
| `NO_PAT_BELOW_VWAP` | VWAP 아래 (가장 많음) |
| `NO_PAT_A_RATE(+X.X%)` | A조건 등락률 부족 |
| `NO_PAT_B_REBOUND_SMALL(+X.X%)` | B조건 반등폭 부족 |
| `NO_PAT_C_MA_FAIL` | C조건 MA5>MA20 미충족 |
| `NO_PAT_C_PULLBACK(X.X%)` | C조건 눌림폭 범위 이탈 |
| `NO_PAT_PR_LOW_FAIL` | PR 저점 우상향 미충족 |
| `NO_PAT_PR_VOL_WEAK` | PR 거래량 팽창 미충족 |
| `NO_PAT_V_FAIL` | V자 감지 실패 |

분석 결과 예시 (2026-05-29 기준):
```
NO_PAT_BELOW_VWAP    70.9%  ← 최대 병목
NO_PAT_A_RATE        11.2%
NO_PAT_B_REBOUND_SMALL  8.9%
NO_PAT_C_MA_FAIL      5.0%
```

### 5.2 V자 실패 사유 단계별 로그

V자 감지 실패 시 어느 조건에서 탈락했는지 분당 1회 기록합니다.

```
[V_FAIL] 010170 | 낙폭부족(+0.0%,최소-3.0%) / 거래량부족(x0.0,최소x1.5)
[V_FAIL] 005930 | VWAP미회복 / MA5조건미충족
```

### 5.3 조건검색 이름 로그

조건검색 편입/편출 시 조건식 이름을 함께 표시합니다.

```
[COND] [자동매매_돌파형A] 초기 결과: 5개 종목
[COND] [자동매매_눌림목_PR] 편입: 010170
```

---

## 6. 손익 계산 개선

### 6.1 잔고API 평균매입단가 기반 손익

매도 시 잔고API의 `pur_pric`(평균매입단가)를 `avg_buy_price`로 `trades.csv`에 기록합니다. 기존 주문가 기반 손익보다 HTS 손익에 더 근접합니다.

### 6.2 전일 이월 포지션 분리

당일 매수 기록이 없는 매도 종목을 "전일 이월"로 분리하여 손익 과대계산을 방지합니다.

```
전일 이월 매도: 000660, 005930  (손익 미집계)
```

### 6.3 리포트 타이밍 개선

리포트 생성을 15:20 → 15:25로 지연하여 마지막 체결이 완전히 기록된 후 생성합니다.

---

## 7. 매도 상세 로그 추가

### 7.1 [SELL_SCORE] 로그

추세꺾임 점수제 발동 시 상세 정보를 기록합니다.

```
[SELL_SCORE] 018880 profit=+1.20% score=4/5 조건=[RSI 72.1·RSI↓·MACD축소·VWAP이탈] VWAP=아래 MA5=위
```

### 7.2 [TRAIL] 로그

구간형 트레일링 발동 시 상세 정보를 기록합니다.

```
[TRAIL] 018880 profit=+0.20% high=5,900원 trail_band=0.8% stop=5,853원
```

### 7.3 analyze_trades.py 섹션 10 추가

거래 분석 리포트에 점수제/트레일링 자동 집계 섹션이 추가됩니다.

```
── 10. 추세꺾임 점수제 분석 ──
  추세꺾임 청산 건수    2건
  승률              1/2 (50%)

── 10-1. 구간형 트레일링 분석 ──
  트레일링 청산 건수    6건
  -0.8% 트레일링     3건  승률 33%  평균 -0.15%
  -1.2% 트레일링     2건  승률 50%  평균 +0.12%
  -2.0% 트레일링     1건  승률 100%  평균 +0.56%
```

---

## 8. B 단독 조건 비활성화

2일 연속 B 단독 신호 손실 확인 후 비활성화하였습니다.

- `pass_rebound = False` 처리 (주석으로 보존)
- A/B, B/C, B/PR 등 조합은 점수에 계속 반영
- 데이터 축적 후 재활성화 여부 결정 예정

---

## 9. 현재 버전 로그 태그 체계

| 태그 | 의미 |
|---|---|
| `[BUY]` | 매수 신호 발생 |
| `[SELL]` | 매도 신호 발생 |
| `[SELL_SCORE]` | 추세꺾임 점수제 상세 (신규) |
| `[TRAIL]` | 트레일링 발동 상세 (신규) |
| `[HOLD_POS]` | 보유 중 상태 |
| `[NEAR_TP]` | 익절가 근접 경고 |
| `[NEAR_SL]` | 손절가 근접 경고 |
| `[BLOCK]` | 분봉 필터 차단 |
| `[HOLD]` | 일반 홀딩 |
| `[MIN]` | 분봉 분석 결과 |
| `[ORDER]` | 주문 접수 완료 |
| `[FAIL]` | 주문 실패 |
| `[COOL]` | 재진입 쿨다운 중 |
| `[EXCL]` | 자동 제외 |
| `[REGIME]` | 장세 판단 결과 |
| `[COND]` | 조건검색 편입/편출 (신규) |
| `[V_FAIL]` | V자 감지 실패 사유 (신규) |
| `[LOSS_CNT]` | 당일 손실 횟수 (신규) |
| `[SYMBOL_COOLDOWN]` | 종목별 쿨다운 발동 (신규) |
| `[SYMBOL_BLOCK_TODAY]` | 당일 매수 금지 발동 (신규) |
| `[RECONCILE]` | 시작 시 state 동기화 |
| `[REPORT]` | 일일 리포트 생성 |
| `[ANALYSIS]` | 분석 스크립트 실행 |
| `[REPLAY]` | 리플레이 실행 |
| `[VALIDATE]` | 로그 품질 검증 |
| `[WAIT]` | 장 외 시간 대기 |

---

## 10. 현재 주요 설정값

| 설정 | 값 | 설명 |
|---|---|---|
| `order_cash_per_trade` | 500,000원 | 1회 주문 금액 |
| `max_positions` | 5종목 | 최대 동시 보유 |
| `stop_loss_pct` | 1.5% | 손절 기준 |
| `trailing_start_pct` | 0.5% (코드 고정) | 트레일링 시작 기준 |
| `trailing_stop_pct` | 구간별 동적 | 0.8~2.0% |
| `take_profit_pct` | 15.0% | 안전망 익절 |
| `trend_reversal_rsi` | 70.0 | 추세꺾임 RSI 기준 |
| `reentry_cooldown_seconds` | 600초 | 기본 재진입 쿨다운 |
| `condition_seqs` | [1, 2, 3] | 조건검색 동시 구독 |
| `v_low_max_age` | 8봉 | V자 저점 탐색 범위 |
| `pullback_max_pct` | -0.3% | 눌림폭 상한 |

---

## 11. 다음 버전 예정 방향

| 우선순위 | 항목 | 내용 |
|---|---|---|
| 🔴 높음 | 재진입 제한 시뮬레이션 | 실제 차단 전 가상 로그로 효과 검증 |
| 🔴 높음 | WATCHLIST 비중 축소 | 제외 후보 종목 주문금액 50% 축소 |
| 🟡 중간 | pullback_max_pct 완화 | -0.3% → -0.5~-0.8% 리플레이 비교 후 결정 |
| 🟡 중간 | A/B 패턴 강화 | 추가 조건 요구 또는 최소점수 상향 |
| 🟡 중간 | condition_name별 성과 분석 | 조건검색식별 BUY전환율/승률/손익 집계 |
| 🟢 낮음 | V자 조건 완화 | V_FAIL 로그 분석 후 병목 조건 선택적 완화 |
| 🟢 낮음 | SKIP_SCORE_TOO_LOW 사후 분석 | 탈락 종목 이후 수익률 확인 |
| 🟢 낮음 | 텔레그램 알림 | 매수/매도/일일리포트 자동 발송 |

---

## 요약

이번 ver1.4 작업을 통해 다음이 완료되었습니다.

- 1분봉 저장 + 리플레이 + 자동 분석으로 **데이터 기반 전략 검증 체계** 완성
- 3개 조건검색 동시 구독으로 **후보군 다양화**
- 점수제 매도 + 구간형 트레일링으로 **조기청산 및 수익 반납 방지**
- 동일 종목 재진입 제한으로 **반복 손실 방어**
- SKIP_NO_PATTERN 세분화 + V자 실패 로그로 **병목 조건 가시화**
- 잔고API 평균매입단가 기반 손익으로 **HTS 손익과 근접한 리포트**

ver1.4는 전략 조건을 감으로 바꾸던 단계에서, **데이터로 병목을 확인하고 하나씩 조정하는 단계**로 전환된 버전입니다.
